# Server Configuration
NODE_ENV=production
PORT=8080

# Database (PostgreSQL)
# Formato: postgresql://usuario:senha@host:porta/nome_do_banco
# Exemplo Railway: postgresql://postgres:senha@containers-us-west-123.railway.app:5432/railway
DATABASE_URL=postgresql://user:password@localhost:5432/route_optimizer

# JWT Authentication
# IMPORTANTE: Use um secret forte em produção! Gere com: openssl rand -base64 32
JWT_SECRET=seu_jwt_secret_super_secreto_aqui_troque_isso
JWT_EXPIRES_IN=7d

# OpenRouteService API (OPCIONAL - Geocoding e Distance Matrix)
# Grátis: 2000 requests/dia
# Criar conta: https://openrouteservice.org/dev/#/signup
# Se não configurar, usa Nominatim (OSM) + Haversine (menos preciso mas 100% grátis)
OPENROUTE_API_KEY=

# Notas:
# - Se você não tiver OPENROUTE_API_KEY, o sistema funciona normalmente
#   usando Nominatim (OpenStreetMap) para geocoding e Haversine para distâncias
# - Haversine calcula distância em linha reta, não distância real de rua
# - Para produção, recomenda-se ter uma key do OpenRouteService (free tier é suficiente)
