[build]
builder = "nixpacks"
[deploy]
startCommand = "python -m gunicorn web_app:app --bind 0.0.0.0:$PORT --timeout 300 --workers 1"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
